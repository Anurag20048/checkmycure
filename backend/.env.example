DJANGO_SECRET_KEY=change-this-in-development
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
CORS_ALLOWED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
# Optional YOLO eye model:
# EYE_MODEL_PATH=C:\path\to\best.pt

# Optional automatic emergency SMS/WhatsApp delivery.
# Keep these values on the Django server only. Never put them in frontend JavaScript.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_WHATSAPP_FROM=
